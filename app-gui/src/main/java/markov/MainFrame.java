package markov;

import javax.swing.*;

public class MainFrame extends JFrame {
    MainFrame() {
        setTitle("Open Mission: Markov Reward Process");
        setSize(600, 600);
        setVisible(true);
    }
}
